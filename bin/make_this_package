#!/bin/bash

cd ${WORKSPACE_DIR}/$(dirname $1)
while [[ ! -f manifest.xml && $(pwd) != "/" ]]
do
    cd ..
done

#if [ ! -e .clang_complete ]
#then
#    gen_clang_complete.bash "$1"
#else
    make -j
#fi

