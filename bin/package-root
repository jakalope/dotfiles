#!/bin/bash

if [[ $# > 0 ]]; then
    cd $SOURCE_DIR/$(dirname $1)
fi

while [[ ! -e package.xml && ! -e manifest.xml && $(pwd) != "/" ]]
do
    cd ..
done

if [[ $(pwd) != "/" ]]; then
    basename $(pwd)
fi

