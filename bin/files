#!/bin/bash
if [[ $# == 0 ]]; then
    # Trivial case.
    2>/dev/null find ./
    exit 0
fi

pattern="${1}"
shift
while [[ $# > 0 ]]; do
    pattern="${pattern}\|${1}"
    shift
done
template='.*\.\(X\)'
regex="${template/X/${pattern}}"
2>/dev/null find ./ -regex "${regex}"
