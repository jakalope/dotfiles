#!/bin/bash

find_in_dir()
{
    2>/dev/null find "${1}" -regex ".*\.\(h\|c\|hpp\|cpp\|cx\|tpp\)$" -print
}

if [[ $# > 0 ]]; then
    while [[ $# != 0 ]]; do
        find_in_dir "${1}"
        shift
    done
else
    find_in_dir ./
fi

    
